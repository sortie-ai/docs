{{- $site := .Site -}}
# {{ $site.Title }}

> {{ $site.Params.description }}

Sortie is an autonomous coding agent orchestrator. Engineers manage work at the ticket level — Sortie handles the rest: isolated workspaces, retry logic, state reconciliation, tracker integration, and cost tracking. Single binary, zero dependencies, SQLite persistence.

For full documentation in a single file suitable for pasting into an AI assistant, see: {{ $site.BaseURL }}llms-full.txt

Tip for agents: every page link below already ends in `.md` and returns the page body as Markdown, without navigation. The canonical HTML page is the same URL with `.md` replaced by a trailing slash. Requesting that HTML URL with the header `Accept: text/markdown` also returns Markdown, but that rendering is converted from the HTML and still carries the sidebar and page chrome. The `##` headings below are sections, not pages, and have no Markdown version.
{{ range $site.Sections }}
## {{ .Title }}
{{ range .RegularPages }}
- [{{ .Title }}]({{ printf "%s.md" (strings.TrimSuffix "/" .Permalink) }}){{ with .Params.description }}: {{ . }}{{ end }}
{{- end }}
{{ end }}
## Optional
{{ range where $site.RegularPages "Section" "" }}
- [{{ .Title }}]({{ printf "%s.md" (strings.TrimSuffix "/" .Permalink) }}){{ with .Params.description }}: {{ . }}{{ end }}
{{- end }}
