# Hugo build output
public/
resources/
.hugo_build.lock

# Hugo vendored modules (generated by `hugo mod vendor`)
_vendor/

# Node.js
node_modules/

# Local tooling (asdf) — Cloudflare uses PYTHON_VERSION env var instead
.tool-versions
