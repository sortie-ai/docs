# Generated by scripts/update-star-count.sh - do not edit by hand.
#
# Rendered by layouts/_partials/navbar.html. The Cloudflare build runs the
# script and overwrites this file in its own workspace, so the committed
# value is the fallback used when that fetch fails - not what the site
# normally shows.
stars: 126
